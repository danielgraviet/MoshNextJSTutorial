'use client';
import React from 'react'

const AddToCart = () => {
  return (
    <div>
        <button onClick={() => console.log("added")}>Add To Cart</button>
    </div>
  )
}

export default AddToCart